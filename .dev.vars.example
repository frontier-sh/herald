# Local dev vars (optional). GitHub App credentials are created by the in-app
# setup wizard and stored in D1 — there are no GitHub secrets to set here.

# Public origin of the deployment. Auto-detected from the request if unset;
# set it when you need stable absolute URLs locally (e.g. behind a tunnel).
BASE_URL=
